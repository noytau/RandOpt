FROM vllm/vllm-openai:v0.11.0

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update && apt-get install -y \
    wget \
    software-properties-common \
    build-essential \
    python3-dev \
    python3-setuptools \
    make \
    cmake \
    ffmpeg \
    libavcodec-dev \
    libavfilter-dev \
    libavformat-dev \
    libavutil-dev \
    python3-pip \
    python3-venv \
    git \
    screen \
    tmux \
    htop \
    zsh \
    unzip \
    && apt-get clean && rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir --upgrade pip setuptools wheel
RUN wget -nv -O - https://raw.githubusercontent.com/zimfw/install/master/install.zsh | zsh
RUN pip install --no-cache-dir git+https://github.com/apple/ml-flextok.git
RUN pip install --no-cache-dir git+https://github.com/openai/CLIP.git

RUN pip install --no-cache-dir \
    datasets \
    huggingface-hub \
    "ray[default]>=2.0.0" \
    pandas \
    pyarrow \
    tqdm \
    wandb
# TODO: add transformers torchvision Pillow here and rebuild image once vision
# correspondence experiment is validated end-to-end. Until then these are
# installed at job runtime (adds ~2 min per job).
# docker build -f RandOpt/docker/Dockerfile_vllm -t noyhassid/randopt-vllm:latest .
# docker push noyhassid/randopt-vllm:latest

WORKDIR /storage/noy/RandOpt
